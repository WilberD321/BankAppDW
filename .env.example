DATABASE_URL=postgresql+psycopg2://<username>:<password>@localhost:5432/bankapp
JWT_SECRET_KEY=<random-secret-key>
# Dev-friendly default so Postman sessions don't expire mid-testing; shorten this for production.
JWT_EXPIRE_MINUTES=120
